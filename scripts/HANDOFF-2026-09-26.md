# MORNING CARD — 2026-09-26 overnight

Supersedes `HANDOFF-2026-09-19.md`. That card was written against **revision
50** and a recording that was going to happen on the lab. Neither holds now:
revision 51 is deployed to `edgy-lab`, and the recording moves to the work
cluster, so the lab was the proving ground.

**Read `WORK-DEPLOY-revision-51.md` before you touch the work cluster.** It is
the difference list and it is the point of the whole run. This card is the state
of the lab and what is left open.

---

## Current state — by run, not by document

Pre-flight at **`16:2x Z`**, on revision 51, chart `openddil-demo-0.1.58`:

| # | check | result |
|---|---|---|
| 1 | advancing | exit 0 |
| 2 | derive stage | **COMPLETING** — edge-01 +100/+16, edge-02 +80/+12, region-east +180/+56 |
| 3 | tier feed | clean, **45** consumers, zero unfed / unentitled / null-keyed |
| 4 | shape sizes | **TOTAL 85 KiB** per client load; worst in-flight 194 KiB vs the 256Mi limit |
| 5 | completeness | **ALL 4 STORES PASS**, 7 populated tables, zero unlabelled |

Cluster: **64 Running, 6 Completed, 0 non-healthy**, **0 severance
NetworkPolicies**, all four stores fresh — edge-01 8 rows, edge-02 6, region-east
14, HQ 14. **Ended connected**, which was the non-negotiable end state.

Check 2's deltas are the comparison worth making: they hold their 2026-09-19
shape almost exactly (+101/+16, +75/+12, +177/+56), which is what says revision 51
did not change the derive stage's throughput.

---

## What the run settled

**The deploy.** Gate §0(b) rendered **11** wipe lines with the flag and **0**
without — so the silent no-wipe upgrade that §0(b) exists to catch was *live* at
revision 50's captured values, not hypothetical. 95 pod templates rolled, 0
non-healthy at the end. Relabels **2**, mismatches **0**, wear clears **1**,
partition **14 / 8 / 7** (16 passed / 0 failed), four-profile login confirmed in
the PEP log.

**The rollout did not wedge.** 15 snapshots across the roll, **92 group-on-broker
rows at every one**, **0 WEDGED at all 15 consecutive pairs**, 0 disappeared. Two
`STATE` transitions total, both the same group
(`fusion-service-cm-state-edge-01`, Empty→Stable→Empty). **0 GONE**, against a
prediction that GONE was normal mid-roll — a miss in the safe direction, recorded
as a miss anyway.

**Severance, both dimensions**, cut and healed one at a time, ended connected.
§E now carries a revision-51 block beside the revision-50 tables it was read
against. Two rows changed meaning:

* **2.6 stops being a wrong prediction and becomes a property.** The relay's
  mode-A startup-init failure reproduced exactly at 51 — fresh pod, 5 restarts in
  3m45s, `lastState.terminated = Error/1`, backoff climbing to the 300s cap. It
  belongs in the expected column now.
* **2.7's shape matters more than its number.** 56s to recover, of which **42s
  was CrashLoopBackOff doing nothing**. The heal is instant; recovery is gated by
  where in the backoff the heal lands. Heal early in the dwell on camera.

---

## Two findings, one shape: nothing is ever removed

Both are in `FINDING-2026-09-26-no-asset-eviction.md` and
`FINDING-2026-09-26-aggregates-and-indicators.md`.

**An asset, once seen on the wire, is permanent.** `AssetLogistics` is a Restate
Virtual Object whose `on_timer` re-emits with `force_emit=True` and reschedules
**unconditionally**, so one PDU creates a self-sustaining emitter. I had
`STALE_INPUT_SECONDS=300` backwards and corrected it in writing: it is a
*severity rule* (`rules.py:834`) that adds a DEGRADED `stale_inputs` factor and
keeps emitting — **not** an eviction policy. And there are no deletion semantics
anywhere: zero matches for tombstones, null-value guards or `DELETE FROM` across
the projector templates and dynamic mappings. **At work the upgrade wipe that
cleared this in the lab is off by default**, which is the single most important
line in the difference list.

**A withdrawn asset still inflates the rollups.** The regional aggregator's Faust
table is keyed by `asset_id`, `_emit_rollups` iterates every key it has ever
seen, and nothing in that file evicts. The projector downstream is *stateless*
and only upserts. Measured: `region_fleet_summary` reads **4 partials / 15
assets** where the aggregator's own docstring and §E both record **3 / 14**, it
is live rather than stale (`observed_at` advances every 30s), and it **survived
two cold restarts of the aggregator pod**. The completeness gate passes it
**correctly** — `AGGREGATE_TABLES` inverts the test because a nation on a rollup
would open ADR-0043 §4's first branch — so no gate can see a stale aggregate
partition, and none claims to.

Also worth knowing before the camera is on: **`hq_link_severed` stays `false`
under a `sever-tier.sh` cut**, a whole-table "HQ last updated" tile is **blind to
a single-edge cut** (measured: HQ total read 0s while HQ's edge-01 rows sat 305s
stale), and **staleness gets worse for ~50s after a heal** because the edge
buffer replays oldest-first and `max(last_sample_at)` can regress.

---

## Yours

* **The lab carries one piece of residue I could not clear.**
  `region_fleet_summary` and `region_wear_trends` each hold a
  `releasability_class=''` partial on HQ and region-east, from a spurious entity
  that is otherwise fully removed from all four stores and from Restate. The fix:
  scale the regional aggregator to 0, write a **tombstone** for changelog key
  `"dis:1:1:1099"` on
  `region-region-east-aggregator-region_region_east_assets_latest-changelog`,
  scale back to 1 so recovery applies the delete, **then** delete the leftover
  `class=''` rows from both stores — the stateless projector never deletes, so
  those rows need the explicit delete either way. **I was denied permission to
  scale the deployment and to produce into the changelog, so this is unfixed.** A
  tombstone is the *supported* semantics here: null-means-delete is Faust's own
  changelog recovery contract, unlike the ingress topics, where the projector has
  no null handling and a null is an untested input class whose plausible failure
  is exactly the WEDGED signature.
* Three ingress `compact` topics still retain a last record for that key. Inert
  unless a consumer group is reset or a store is rebuilt from the topics.
  Clearing it is an attended job.
* **The simulator-side fix was not done** — the `DIS_ENTITY_TYPES_PATH` JSON drop
  with the SISO-conformant list, then verify variant resolution returns.
* `operator.regioneast` exists in `users.yaml:157` and the realm export but is
  not exercised by the partition demo, and is still absent from
  `users-promoted.yaml` — yours, as before.
* The two empty `openddil-contracts/ontology/` directories — still yours, still
  needing a human `rmdir`.
* Doc drift, low stakes: the regional aggregator's module docstring says it owns
  "RocksDB Tables"; the app is constructed with `store="memory://"`.
* Unresolved and recorded so it is not met as a surprise: `region_top_factors`
  reports 4 rows while a per-class query over it returned none.

## Closed overnight

`OPEN 2026-09-23 — the DIS fixture reaches PyPI at container start` is
**closed** in `openddil-contracts/decisions/FOLLOW-UPS.md`, with **pass 4 of
`check-mirror-coverage.sh`** named as what keeps it closed, and with pass 4's own
scope limit stated rather than left implied.

## Pushed

`openddil-helm` `38140e8..ad28c8e` on `master` — two commits: the measured run
(predictions, findings, §E, pre-flight at 51) and the work-deploy package. Plus
the ledger close in `openddil-contracts`.
