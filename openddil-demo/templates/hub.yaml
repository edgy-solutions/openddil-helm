{{- /*
   hub.yaml — HQ-tier services that depend on infrastructure (postgres-hq,
   redpanda-hq, toxiproxy) being up:
     * electric-sync (ElectricSQL HTTP gateway over postgres-hq replication)
     * restate-server (StatefulSet with PVC for /restate-data)
     * restate-hub (consumer bridge — mounts restate_hub.py from bundle)
     * cm-service + cm-service-bootstrap (post-install Job)
     * asset-registry-service (ADR-0028: canonical writer for
       public.asset_registry; one pod, multi-broker Faust composition
       consuming telemetry-latest-state from every edge, publishes
       asset-registry-events on HQ broker for consumer cache pub/sub)
     * logistics-fusion-service + logistics-fusion-bootstrap (post-install Job)
     * projector-hq (HQ-cluster topic projection to postgres-hq)
     * frontend (nginx-served static + /proxies/ proxy to toxiproxy)
*/}}
{{- $hqBroker := printf "%s-redpanda-hq%s:%d" .Release.Name (include "openddil.svcDomain" .) (int .Values.redpandaHq.kafkaPort) }}
{{- $pgDsn := printf "postgres://%s:%s@%s-postgres-hq%s:%d/%s"
    .Values.postgresHq.auth.username
    .Values.postgresHq.auth.password
    .Release.Name
    (include "openddil.svcDomain" .)
    (int .Values.postgresHq.port)
    .Values.postgresHq.auth.database }}
{{- $pgDsnElectric := printf "postgresql://%s:%s@%s-postgres-hq%s:%d/%s"
    .Values.postgresHq.auth.username
    .Values.postgresHq.auth.password
    .Release.Name
    (include "openddil.svcDomain" .)
    (int .Values.postgresHq.port)
    .Values.postgresHq.auth.database }}
{{/*
THE DETECTION CUTOVER — UD-10, and ADR-0026's "coupled interim mechanisms
retire together" applied to the detection reachback.

This list drives the root's cm-service and fusion Restate subscriptions:
for every edge in it, the ROOT reaches DOWN into that edge's broker and
computes CM state and severity from its raw stream. That downward reach was
the ADR-0034 interim — detection ran once, centrally, because no tier had a
detection plane of its own. Tier-local detection is its REPLACEMENT, and a
replacement that leaves the interim running is not a replacement.

⚠ AN EDGE WITH A TIER NODE IS SKIPPED HERE, AND THE ALTERNATIVE IS WORSE
THAN IT LOOKS. Leaving it in produced UD-10: the tier's Restate and the
root's Restate registered the SAME consumer-group id against the SAME
one-partition topic, so Kafka gave the partition to exactly one of them and
the other went silent while reporting healthy.

The obvious repair — give the tier a distinct group id — is REJECTED, and
not merely because it leaves the design undecided. Distinct groups make BOTH
consumers receive every message, so the tier computes severity for this edge
AND the root computes severity for this edge, independently, from one raw
stream. Two severity truths for one asset, arbitrated by whichever projector
wrote last. That is the two-truths failure this project has refused every
time it appeared — two authz corpora, two read paths, two policy stores —
arriving in the detection plane wearing a one-line config fix.

What the root gets instead is the tier's DERIVED state, carried up by the
bridge (see edge.yaml): `asset-logistics-status` and `asset-cm-state`, with
releasability labels already stamped by the tier's own fusion and
cm-service. Same topic names at HQ, different producer — so the root's
projector and its HQ-cluster fusion subscription need no change at all.

Resolves GD-08 for tier-managed edges as a side effect, which is a sign this
is the right cut rather than a workaround for the symptom.
*/}}
{{- $cmEdgeClusters := list }}
{{- range .Values.edges }}
{{- if not (include "openddil.isTierManaged" (dict "id" .id "root" $)) }}
{{- $cmEdgeClusters = append $cmEdgeClusters (printf "%s=%s-redpanda-%s:%d" .id $.Release.Name .id (int $.Values.redpandaEdge.internalPort)) }}
{{- end }}
{{- end }}

# ===========================================================================
# electric-sync — ElectricSQL HTTP gateway
# ===========================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-electric-sync
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
    {{- include "openddil.selectorLabels" (dict "component" "electric-sync" "root" .) | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "openddil.selectorLabels" (dict "component" "electric-sync" "root" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openddil.labels" . | nindent 8 }}
        {{- include "openddil.selectorLabels" (dict "component" "electric-sync" "root" .) | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: electric
          image: "{{ include "openddil.thirdPartyImage" .Values.electric.image }}"
          imagePullPolicy: {{ include "openddil.pullPolicy" (dict "policy" .Values.electric.image.pullPolicy "root" .) }}
          env:
            - name: DATABASE_URL
              value: {{ $pgDsnElectric | quote }}
            - name: LOGICAL_PUBLISHER_HOST
              value: {{ printf "%s-electric-sync%s" .Release.Name (include "openddil.svcDomain" .) | quote }}
            - name: AUTH_JWT_ALG
              value: "HS256"
            - name: AUTH_JWT_KEY
              value: "secret"
            - name: ELECTRIC_INSECURE
              value: "true"
            # ElectricSQL bind port — explicit so it matches the Service.
            - name: ELECTRIC_PORT
              value: {{ .Values.electric.port | quote }}
          ports:
            - name: http
              containerPort: {{ .Values.electric.port }}
          resources:
            {{- toYaml .Values.electric.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-electric-sync
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "openddil.selectorLabels" (dict "component" "electric-sync" "root" .) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.electric.port }}
      targetPort: http
---
# Alias Service so the frontend nginx's hardcoded `electric-sync:5133`
# upstream resolves (used by the /electric/ proxy_pass block added when
# VITE_ELECTRIC_URL was made build-time-overridable). Same pattern as
# the `toxiproxy` alias below — unprefixed name matching docker-compose's
# container hostname so nginx.conf is portable between the two envs.
apiVersion: v1
kind: Service
metadata:
  name: electric-sync
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
    openddil.io/alias: "true"
spec:
  selector:
    {{- include "openddil.selectorLabels" (dict "component" "electric-sync" "root" .) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.electric.port }}
      targetPort: http
---
# ===========================================================================
# restate-config — GENERATED from declared topology, not copied from the bundle
# ===========================================================================
# ROOT-TIER CONVERGENCE ON TIER-NODE MECHANICS (GD-09, ADR-0033).
#
# Restate 1.6.x has no runtime POST /clusters admin API, so Kafka clusters
# must exist in the SERVER CONFIG at boot. The tier node established the
# conformant answer to that constraint — render the config from values (see
# tier-node.yaml tier-restate-config) — and this is the root tier adopting it.
#
# The bundle's baked demo/restate-config.toml hardcoded three cluster names
# (openddil-edge-01/02/03) and docker-compose broker hostnames
# (`redpanda-edge-01:9092`). An overlay naming its edges anything else failed
# subscription creation with "specified cluster in the source URI does not
# exist" — the cluster set is topology, which ADR-0033 says is deployment
# configuration.
#
# The bare hostnames themselves DID resolve: infrastructure.yaml publishes
# unprefixed alias Services precisely so compose-era configs keep working.
# Generating from values removes the dependency on that shim rather than
# repairing a break — the addresses below are release-prefixed and correct
# with or without the aliases.
#
# Generating from .Values.edges means the cluster set follows the DECLARED
# topology automatically: N edges produce N clusters, named and addressed
# correctly, with no artifact to keep in sync. Adding or renaming an edge is
# a values change and nothing else.
#
# Subscription source URIs are kafka://openddil-<edge_id>/<topic>, so the
# `openddil-` prefix here is load-bearing — it must match what the bootstrap
# scripts construct.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-restate-config
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
data:
  restate-config.toml: |
    # Restate server configuration for the openddil-demo stack.
    # GENERATED by the chart from .Values.edges — do not bake a copy.
    #
    # ADR-0023 Phase 6b §A: Kafka clusters are defined statically so the
    # bootstrap scripts' subscription source URIs (kafka://openddil-<edge_id>/…)
    # resolve to a known cluster.
    {{- range .Values.edges }}

    [[ingress.kafka-clusters]]
    name = "openddil-{{ .id }}"
    brokers = ["{{ printf "%s-redpanda-%s%s:%d" $.Release.Name .id (include "openddil.svcDomain" $) (int $.Values.redpandaEdge.internalPort) }}"]
    {{- end }}

    [[ingress.kafka-clusters]]
    name = "openddil-hq"
    brokers = ["{{ printf "%s-redpanda-hq%s:%d" .Release.Name (include "openddil.svcDomain" .) (int .Values.redpandaHq.kafkaPort) }}"]
---
# ===========================================================================
# restate-server — StatefulSet with PVC for /restate-data
# ===========================================================================
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Release.Name }}-restate-server
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
    {{- include "openddil.selectorLabels" (dict "component" "restate-server" "root" .) | nindent 4 }}
spec:
  serviceName: {{ .Release.Name }}-restate-server
  replicas: 1
  selector:
    matchLabels:
      {{- include "openddil.selectorLabels" (dict "component" "restate-server" "root" .) | nindent 6 }}
  template:
    metadata:
      annotations:
        # Restate reads its Kafka cluster set at BOOT — there is no runtime
        # reload. Without this, adding or renaming an edge would update the
        # ConfigMap while the running server kept the old cluster list, and
        # subscription creation would fail against a cluster the operator can
        # plainly see in the config. Hashing the declared topology forces the
        # restart that makes the new set take effect.
        checksum/restate-config: {{ .Values.edges | toYaml | sha256sum }}
      labels:
        {{- include "openddil.labels" . | nindent 8 }}
        {{- include "openddil.selectorLabels" (dict "component" "restate-server" "root" .) | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        # No bundle-loader initContainer — the config is generated above and
        # mounted directly.
        - name: restate
          image: "{{ include "openddil.thirdPartyImage" .Values.restate.image }}"
          imagePullPolicy: {{ include "openddil.pullPolicy" (dict "policy" .Values.restate.image.pullPolicy "root" .) }}
          args: ["-c", "/config/restate-config.toml"]
          ports:
            - name: ingress
              containerPort: {{ .Values.restate.ingressPort }}
            - name: admin
              containerPort: {{ .Values.restate.adminPort }}
          volumeMounts:
            - name: restate-config
              mountPath: /config/restate-config.toml
              subPath: restate-config.toml
            - name: data
              mountPath: /restate-data
          resources:
            {{- toYaml .Values.restate.resources | nindent 12 }}
      volumes:
        - name: restate-config
          configMap:
            name: {{ .Release.Name }}-restate-config
        {{- if .Values.persistence.restateUseEmptyDir }}
        # NFS-incompatible cluster mode (same rationale as redpanda above):
        # restate's RocksDB requires POSIX file locks that NFS doesn't
        # honor, so on cluster where only NFS PVCs are available we run
        # restate on a node-local emptyDir. Trade-off: a pod restart loses
        # ALL Virtual Object state -- which we have a separate story for
        # (`restate.ephemeralOnUpgrade` + flush-assets.sh --deep), so the
        # state-loss isn't a new failure mode. Toggle via
        # .Values.persistence.restateUseEmptyDir.
        #
        # sizeLimit mirrors persistence.restateSize, for the reason spelled
        # out on the redpanda-edge StatefulSet: the PVC path it replaces is
        # bounded, so the escape hatch must be bounded too, and an unbounded
        # emptyDir turns a component-local disk problem into node-wide
        # eviction of whichever pods happen to be using the most space.
        - name: data
          emptyDir:
            sizeLimit: {{ .Values.persistence.restateSize }}
        {{- end }}
  {{- if not .Values.persistence.restateUseEmptyDir }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ReadWriteOnce]
        volumeMode: Filesystem
        {{- with (.Values.persistence.restateStorageClass | default .Values.persistence.storageClass) }}
        storageClassName: {{ . }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.persistence.restateSize }}
  {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-restate-server
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "openddil.selectorLabels" (dict "component" "restate-server" "root" .) | nindent 4 }}
  ports:
    - name: ingress
      port: {{ .Values.restate.ingressPort }}
      targetPort: ingress
    - name: admin
      port: {{ .Values.restate.adminPort }}
      targetPort: admin
---
# ===========================================================================
# restate-hub — consumer bridge. NOTE: docker-compose mounts restate_hub.py
# over /app/restate_hub.py because the published hub-restate-projector image
# bakes hub_restate_projector.py, not restate_hub.py. We do the same.
# ===========================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-restate-hub
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
    {{- include "openddil.selectorLabels" (dict "component" "restate-hub" "root" .) | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "openddil.selectorLabels" (dict "component" "restate-hub" "root" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openddil.labels" . | nindent 8 }}
        {{- include "openddil.selectorLabels" (dict "component" "restate-hub" "root" .) | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- include "openddil.bundleInit" (dict "paths" (list
            (dict "src" "agents/hub/restate_hub.py" "dst" "restate_hub.py")
          ) "root" .) | nindent 8 }}
      containers:
        - name: restate-hub
          image: {{ include "openddil.image" (dict "name" .Values.restateHub.image.name "tag" .Values.restateHub.image.tag "digest" .Values.restateHub.image.digest "root" .) }}
          imagePullPolicy: {{ include "openddil.pullPolicy" (dict "policy" .Values.restateHub.image.pullPolicy "root" .) }}
          command: ["/opt/venv/bin/python", "/app/restate_hub.py"]
          env:
            - name: DB_PATH
              value: "/tmp/edge_state.db"
          resources:
            {{- toYaml .Values.restateHub.resources | nindent 12 }}
          volumeMounts:
            - name: bundle-shared
              mountPath: /app/restate_hub.py
              subPath: restate_hub.py
      volumes:
        - name: bundle-shared
          emptyDir: {}
---
# ===========================================================================
# cm-service — Configuration Management on Restate
# ===========================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-cm-service
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
    {{- include "openddil.selectorLabels" (dict "component" "cm-service" "root" .) | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "openddil.selectorLabels" (dict "component" "cm-service" "root" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openddil.labels" . | nindent 8 }}
        {{- include "openddil.selectorLabels" (dict "component" "cm-service" "root" .) | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- include "openddil.bundleInit" (dict "paths" (list
            (dict "src" "contracts/gen/python" "dst" "proto")
            (dict "src" "contracts/baselines"  "dst" "baselines")
          ) "root" .) | nindent 8 }}
      containers:
        - name: cm-service
          image: {{ include "openddil.image" (dict "name" .Values.cmService.image.name "tag" .Values.cmService.image.tag "digest" .Values.cmService.image.digest "root" .) }}
          imagePullPolicy: {{ include "openddil.pullPolicy" (dict "policy" .Values.cmService.image.pullPolicy "root" .) }}
          env:
            # cm-service multi-directory baseline support (CM_BASELINES_DIRS)
            # is intentionally NOT wired here. Customer overlays that want to
            # layer additional baselines follow the same overlay-patch pattern
            # the frontend + projector overlays use: a post-install Job in
            # the customer chart `kubectl patch`es this Deployment to add a
            # /baselines-overlay volumeMount + CM_BASELINES_DIRS env. Keeps
            # the OSS chart customer-agnostic. See
            # openddil-customer-bundle-customer-overlay/chart/templates/
            # hook-patch-cm-service.yaml for the canonical example.
            - name: CM_BASELINES_DIR
              value: "/baselines"
            - name: CM_KAFKA_BROKERS
              value: {{ $hqBroker | quote }}
            - name: CM_HTTP_PORT
              value: {{ .Values.cmService.port | quote }}
            - name: CM_STALENESS_WINDOW_S
              value: "900"
            - name: LOG_LEVEL
              value: "INFO"
            - name: PYTHONPATH
              value: "/proto:/app/src"
          ports:
            - name: http
              containerPort: {{ .Values.cmService.port }}
          volumeMounts:
            - name: bundle-shared
              mountPath: /proto
              subPath: proto
            - name: bundle-shared
              mountPath: /baselines
              subPath: baselines
          resources:
            {{- toYaml .Values.cmService.resources | nindent 12 }}
      volumes:
        - name: bundle-shared
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-cm-service
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "openddil.selectorLabels" (dict "component" "cm-service" "root" .) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.cmService.port }}
      targetPort: http
---
# ===========================================================================
# asset-registry-service (ADR-0028) — canonical asset->edge->region writer
# ===========================================================================
# One pod composes per-edge Faust Apps in-process. Consumes
# telemetry-latest-state from each edge broker, runs edge-assignment to
# compute (edge_id, region_id), upserts asset_registry on HQ postgres,
# publishes asset-registry-events on HQ broker for consumer cache
# invalidation. Single writer for the asset_registry table by design --
# any future static-assignment input lands via a separate admin write
# path, not a second Faust pod.
#
# Bundle init brings in /proto (contracts/gen/python) and
# /etc/openddil/edge-assignment/edge-assignment.yaml (the FOB list +
# strategy chain, same file the projector reads).
#
# Depends on:
#   - postgres-schema-init having applied the 20260604202813 migration
#     (CHECK constraint on assignment_source enforces the four ADR-0028
#     values; older migrations would let the service start but every
#     upsert would fail).
#   - The asset-registry-events topic existing (topic-init creates it).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-asset-registry-service
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
    {{- include "openddil.selectorLabels" (dict "component" "asset-registry-service" "root" .) | nindent 4 }}
spec:
  replicas: 1   # Single writer by design (ADR-0028). DO NOT scale up.
  strategy:
    type: Recreate   # Faust state recovery prefers a stop-then-start cycle.
  selector:
    matchLabels:
      {{- include "openddil.selectorLabels" (dict "component" "asset-registry-service" "root" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openddil.labels" . | nindent 8 }}
        {{- include "openddil.selectorLabels" (dict "component" "asset-registry-service" "root" .) | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- include "openddil.bundleInit" (dict "paths" (list
            (dict "src" "contracts/gen/python" "dst" "proto")
          ) "root" .) | nindent 8 }}
      containers:
        - name: asset-registry-service
          image: {{ include "openddil.image" (dict "name" .Values.assetRegistry.image.name "tag" .Values.assetRegistry.image.tag "digest" .Values.assetRegistry.image.digest "root" .) }}
          imagePullPolicy: {{ include "openddil.pullPolicy" (dict "policy" .Values.assetRegistry.image.pullPolicy "root" .) }}
          env:
            - name: ASSET_REGISTRY_POSTGRES_DSN
              value: {{ $pgDsn | quote }}
            - name: ASSET_REGISTRY_EDGES
              # Comma-separated edge=broker pairs across ALL edges.
              # Each pair becomes one Faust App in the composed worker.
              {{- $edgePairs := list }}
              {{- range $edge := .Values.edges }}
              {{- $edgePairs = append $edgePairs (printf "%s=%s-redpanda-%s%s:%d" $edge.id $.Release.Name $edge.id (include "openddil.svcDomain" $) (int $.Values.redpandaEdge.internalPort)) }}
              {{- end }}
              value: {{ join "," $edgePairs | quote }}
            - name: ASSET_REGISTRY_KAFKA_BROKERS
              value: {{ printf "%s-redpanda-hq%s:%d" .Release.Name (include "openddil.svcDomain" .) (int .Values.redpandaHq.kafkaPort) | quote }}
            - name: ASSET_REGISTRY_INPUT_TOPIC
              value: "telemetry-latest-state"
            - name: ASSET_REGISTRY_OUTPUT_TOPIC
              value: {{ .Values.assetRegistry.outputTopic | quote }}
            # EDGE_ASSIGNMENT_CONFIG is intentionally NOT set in the OSS
            # chart. The OSS chart has no embedded FOB list -- without an
            # overlay, every asset assigns to "unspecified" via the
            # service's built-in fallback (still useful: divergence
            # tracking + asset_id->edge cache for downstream consumers).
            # Customer overlays patch this in via the same post-install
            # hook pattern they use to point the projectors at their
            # FOB file:
            #   kubectl patch deploy/<release>-asset-registry-service \
            #     --type strategic -p '{"spec":{"template":{"spec":{
            #       "containers":[{"name":"asset-registry-service",
            #         "volumeMounts":[{"name":"edge-assignment",
            #           "mountPath":"/etc/openddil/edge-assignment"}],
            #         "env":[{"name":"EDGE_ASSIGNMENT_CONFIG",
            #           "value":"/etc/openddil/edge-assignment/edge-assignment.yaml"}]
            #       }],
            #       "volumes":[{"name":"edge-assignment","configMap":
            #         {"name":"<slug>-edge-assignment"}}]
            #     }}}}'
            - name: ASSET_REGISTRY_WEB_PORT
              value: {{ .Values.assetRegistry.webPort | quote }}
            - name: PYTHONPATH
              # bundle-loader writes contracts/gen/python -> /shared/proto;
              # this container mounts the same volume with subPath=proto at
              # /proto, so the generated bindings live at
              # /proto/openddil/telemetry/v1/telemetry_pb2.py. Python won't
              # see them without /proto on sys.path -- _try_decode_event
              # then silently returns None for every event, ADR-0028's
              # asset-registry-events stays empty, source-app's region
              # cache stays empty, and the whole regional aggregation
              # chain stays dark. Mirrors what cm-service / logistics-
              # fusion / regional / projector all already do.
              value: "/proto:/app/src"
            - name: LOG_LEVEL
              value: "INFO"
          ports:
            - name: faust-web
              containerPort: {{ .Values.assetRegistry.webPort }}
          volumeMounts:
            - name: bundle-shared
              mountPath: /proto
              subPath: proto
          resources:
            {{- toYaml .Values.assetRegistry.resources | nindent 12 }}
      volumes:
        - name: bundle-shared
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-asset-registry-service
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "openddil.selectorLabels" (dict "component" "asset-registry-service" "root" .) | nindent 4 }}
  ports:
    - name: faust-web
      port: {{ .Values.assetRegistry.webPort }}
      targetPort: faust-web
---
# cm-service-bootstrap — Job: registers CM service with Restate + creates
# subscriptions per edge cluster. Helm hook, fires after CM service is up.
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-cm-service-bootstrap
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "20"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 6
  template:
    metadata:
      labels:
        {{- include "openddil.labels" . | nindent 8 }}
        {{- include "openddil.selectorLabels" (dict "component" "cm-service-bootstrap" "root" .) | nindent 8 }}
    spec:
      restartPolicy: OnFailure
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- include "openddil.bundleInit" (dict "paths" (list
            (dict "src" "contracts/bootstrap" "dst" "openddil_bootstrap")
          ) "root" .) | nindent 8 }}
      containers:
        - name: bootstrap
          image: {{ include "openddil.image" (dict "name" .Values.cmService.image.name "tag" .Values.cmService.image.tag "digest" .Values.cmService.image.digest "root" .) }}
          imagePullPolicy: {{ include "openddil.pullPolicy" (dict "policy" .Values.cmService.image.pullPolicy "root" .) }}
          command: ["python", "/app/bootstrap/register_subscriptions.py"]
          env:
            - name: RESTATE_ADMIN_URL
              value: {{ printf "http://%s-restate-server%s:%d" .Release.Name (include "openddil.svcDomain" .) (int .Values.restate.adminPort) | quote }}
            - name: CM_SERVICE_ENDPOINT
              value: {{ printf "http://%s-cm-service%s:%d" .Release.Name (include "openddil.svcDomain" .) (int .Values.cmService.port) | quote }}
            - name: CM_EDGE_CLUSTERS
              value: {{ join "," $cmEdgeClusters | quote }}
            - name: LOG_LEVEL
              value: "INFO"
            - name: PYTHONPATH
              value: "/app"
          resources:
            {{- toYaml .Values.jobs.resources | nindent 12 }}
          volumeMounts:
            - name: bundle-shared
              mountPath: /app/openddil_bootstrap
              subPath: openddil_bootstrap
      volumes:
        - name: bundle-shared
          emptyDir: {}
---
# ===========================================================================
# logistics-fusion-service
# ===========================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-logistics-fusion-service
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
    {{- include "openddil.selectorLabels" (dict "component" "logistics-fusion-service" "root" .) | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "openddil.selectorLabels" (dict "component" "logistics-fusion-service" "root" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openddil.labels" . | nindent 8 }}
        {{- include "openddil.selectorLabels" (dict "component" "logistics-fusion-service" "root" .) | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- include "openddil.bundleInit" (dict "paths" (list
            (dict "src" "contracts/gen/python" "dst" "proto")
            (dict "src" "contracts/ontology"   "dst" "ontology")
            (dict "src" "demo/ontology"       "dst" "ontology" "overlay" true)
          ) "root" .) | nindent 8 }}
      containers:
        - name: logistics-fusion
          image: {{ include "openddil.image" (dict "name" .Values.logisticsFusion.image.name "tag" .Values.logisticsFusion.image.tag "digest" .Values.logisticsFusion.image.digest "root" .) }}
          imagePullPolicy: {{ include "openddil.pullPolicy" (dict "policy" .Values.logisticsFusion.image.pullPolicy "root" .) }}
          env:
            - name: FUSION_KAFKA_BROKERS
              value: {{ $hqBroker | quote }}
            - name: FUSION_HTTP_PORT
              value: {{ .Values.logisticsFusion.port | quote }}
            - name: EMIT_INTERVAL_SECONDS
              value: "30"
            - name: STALE_INPUT_SECONDS
              value: "300"
            - name: ONTOLOGY_DIR
              value: "/ontology"
            - name: LOG_LEVEL
              value: "INFO"
            - name: PYTHONPATH
              value: "/proto:/app/src"
          ports:
            - name: http
              containerPort: {{ .Values.logisticsFusion.port }}
          volumeMounts:
            - name: bundle-shared
              mountPath: /proto
              subPath: proto
            - name: bundle-shared
              mountPath: /ontology
              subPath: ontology
          resources:
            {{- toYaml .Values.logisticsFusion.resources | nindent 12 }}
      volumes:
        - name: bundle-shared
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-logistics-fusion-service
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "openddil.selectorLabels" (dict "component" "logistics-fusion-service" "root" .) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.logisticsFusion.port }}
      targetPort: http
---
# logistics-fusion-bootstrap — Job, hooks at weight 21 (after CM bootstrap)
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-logistics-fusion-bootstrap
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "21"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 6
  template:
    metadata:
      labels:
        {{- include "openddil.labels" . | nindent 8 }}
        {{- include "openddil.selectorLabels" (dict "component" "logistics-fusion-bootstrap" "root" .) | nindent 8 }}
    spec:
      restartPolicy: OnFailure
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- include "openddil.bundleInit" (dict "paths" (list
            (dict "src" "contracts/bootstrap" "dst" "openddil_bootstrap")
          ) "root" .) | nindent 8 }}
      containers:
        - name: bootstrap
          image: {{ include "openddil.image" (dict "name" .Values.logisticsFusion.image.name "tag" .Values.logisticsFusion.image.tag "digest" .Values.logisticsFusion.image.digest "root" .) }}
          imagePullPolicy: {{ include "openddil.pullPolicy" (dict "policy" .Values.logisticsFusion.image.pullPolicy "root" .) }}
          command: ["python", "/app/bootstrap/register_subscriptions.py"]
          env:
            - name: RESTATE_ADMIN_URL
              value: {{ printf "http://%s-restate-server%s:%d" .Release.Name (include "openddil.svcDomain" .) (int .Values.restate.adminPort) | quote }}
            - name: FUSION_SERVICE_ENDPOINT
              value: {{ printf "http://%s-logistics-fusion-service%s:%d" .Release.Name (include "openddil.svcDomain" .) (int .Values.logisticsFusion.port) | quote }}
            - name: FUSION_EDGE_CLUSTERS
              value: {{ join "," $cmEdgeClusters | quote }}
            - name: FUSION_HQ_CLUSTER
              value: {{ $hqBroker | quote }}
            - name: LOG_LEVEL
              value: "INFO"
            - name: PYTHONPATH
              value: "/app"
          resources:
            {{- toYaml .Values.jobs.resources | nindent 12 }}
          volumeMounts:
            - name: bundle-shared
              mountPath: /app/openddil_bootstrap
              subPath: openddil_bootstrap
      volumes:
        - name: bundle-shared
          emptyDir: {}
---
# ===========================================================================
# projector-hq — HQ-cluster topic projection to postgres-hq
# ===========================================================================
# Same image as per-edge projector; different broker (hq), different
# edge/region labels (unspecified), buffer monitor off.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-projector-hq
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
    {{- include "openddil.selectorLabels" (dict "component" "projector-hq" "root" .) | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "openddil.selectorLabels" (dict "component" "projector-hq" "root" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openddil.labels" . | nindent 8 }}
        {{- include "openddil.selectorLabels" (dict "component" "projector-hq" "root" .) | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- include "openddil.bundleInit" (dict "paths" (list
            (dict "src" "contracts/gen/python" "dst" "proto")
          ) "root" .) | nindent 8 }}
      containers:
        - name: projector
          image: {{ include "openddil.image" (dict "name" .Values.projector.image.name "tag" .Values.projector.image.tag "digest" .Values.projector.image.digest "root" .) }}
          imagePullPolicy: {{ include "openddil.pullPolicy" (dict "policy" .Values.projector.image.pullPolicy "root" .) }}
          env:
            - name: KAFKA_BROKERS
              value: {{ $hqBroker | quote }}
            - name: POSTGRES_DSN
              value: {{ $pgDsn | quote }}
            - name: METRICS_PORT
              value: {{ .Values.projector.port | quote }}
            - name: RATE_LIMIT_PER_SEC
              value: "10"
            - name: LOG_LEVEL
              value: "INFO"
            - name: PYTHONPATH
              value: "/proto:/app/src"
            - name: OPENDDIL_EDGE_ID
              value: "edge-unspecified"
            - name: OPENDDIL_REGION_ID
              value: "region-unspecified"
            - name: BUFFER_MONITOR_ENABLED
              value: "false"
          ports:
            - name: metrics
              containerPort: {{ .Values.projector.port }}
          volumeMounts:
            - name: bundle-shared
              mountPath: /proto
              subPath: proto
          resources:
            {{- toYaml .Values.projector.resources | nindent 12 }}
      volumes:
        - name: bundle-shared
          emptyDir: {}
---
# ===========================================================================
# frontend
# ===========================================================================
# The frontend nginx hardcodes `toxiproxy` as the proxy upstream hostname.
# In k8s we satisfy that by adding a SECOND Service named exactly `toxiproxy`
# (no Release.Name prefix) that selects the same toxiproxy pod. See below.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-frontend
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
    {{- include "openddil.selectorLabels" (dict "component" "frontend" "root" .) | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "openddil.selectorLabels" (dict "component" "frontend" "root" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openddil.labels" . | nindent 8 }}
        {{- include "openddil.selectorLabels" (dict "component" "frontend" "root" .) | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: frontend
          image: {{ include "openddil.image" (dict "name" .Values.frontend.image.name "tag" .Values.frontend.image.tag "digest" .Values.frontend.image.digest "root" .) }}
          imagePullPolicy: {{ include "openddil.pullPolicy" (dict "policy" .Values.frontend.image.pullPolicy "root" .) }}
          {{- if .Values.releasability.enabled }}
          env:
            # WHERE THE BROWSER-S READ PATH ACTUALLY GOES.
            #
            # The browser keeps requesting the same same-origin
            # `/electric/v1/shape`; nginx sends it to the PEP instead of to
            # Electric. No frontend rebuild, no CORS, and the session cookie
            # rides along because nothing about the origin changed.
            #
            # This and the NetworkPolicy are set from the SAME value on
            # purpose. If this were left pointing at Electric while
            # enforcement was on, the result would be a complete bypass that
            # looked like everything working — so the NetworkPolicy makes
            # that combination fail visibly instead.
            - name: OPENDDIL_ELECTRIC_UPSTREAM
              value: {{ printf "%s-pep" .Release.Name | quote }}
            - name: OPENDDIL_ELECTRIC_PORT
              value: "8080"
            - name: OPENDDIL_PEP_UPSTREAM
              value: {{ printf "%s-pep" .Release.Name | quote }}
            # THE SHELL REQUIRES A SESSION, not just the data paths.
            # Set from the SAME condition that puts this tier's PEP into
            # oidc mode, because the gate is only satisfiable when something
            # can answer /auth/me: with no PEP that is a 502 and in header
            # mode a 404, and nginx's auth_request turns either into a 500
            # for the whole app. Tying them to one condition is what stops a
            # deployment having a gate with nothing behind it.
            - name: OPENDDIL_SESSION_GATE
              value: {{ ternary "on" "off" .Values.releasability.oidc.enabled | quote }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.frontend.port }}
          resources:
            {{- toYaml .Values.frontend.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "openddil.selectorLabels" (dict "component" "frontend" "root" .) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.frontend.port }}
      targetPort: http
---
# Alias service so frontend nginx's hardcoded `toxiproxy:8475` resolves.
# This is intentionally unprefixed (not {{ .Release.Name }}-toxiproxy) —
# it's a fixed hostname the frontend image expects. One chart install per
# namespace, so the unqualified name is safe.
apiVersion: v1
kind: Service
metadata:
  name: toxiproxy
  labels:
    {{- include "openddil.labels" . | nindent 4 }}
    openddil.io/alias: "true"
spec:
  selector:
    {{- include "openddil.selectorLabels" (dict "component" "toxiproxy" "root" .) | nindent 4 }}
  ports:
    - name: api
      port: {{ .Values.toxiproxy.apiPort }}
      targetPort: api
    - name: proxy
      port: {{ .Values.toxiproxy.proxyPort }}
      targetPort: proxy
